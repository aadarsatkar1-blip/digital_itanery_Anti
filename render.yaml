services:
  - type: web
    name: itanery-django
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn wsgi:application --bind 0.0.0.0:$PORT --timeout 300 --max-requests 500"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
